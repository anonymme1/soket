#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <sys/socket.h>
#include <arpa/inet.h>
#include <netinet/in.h>
#include <unistd.h>

#include "socket.h"

int main() {

/*     < Server >
	Server serv(8080);
	char msg[] = "Hi, i am server";
	char buff[1024] = {0};

	serv.menu(buff, msg);
*/

/*     < Client >
	Server cli(8080);
	char msg[] = "Hi, i am client";
	char buff[1024] = {0};

	cli.menu(buff, msg);
*/

	return 0;
}

