#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <sys/socket.h>
#include <arpa/inet.h>
#include <netinet/in.h>
#include <unistd.h>

#include "socket.h"

int main() {

/*      < SERVER >

	Server serv(8080);
	std::string msg;

	std::cout<<"msg: ";
	getline(std::cin, msg);

	char buff[1024] = {0};

	serv.menu(buff, (char*)&msg);
*/

/*      < CLIENT >

	Server cli(8080);
	std::string msg;

	std::cout<<"Msg: ";
	getline(std::cin, msg);

	char buff[1024] = {0};

	cli.menu(buff, (char*)&msg);
*/
	return 0;
}

